<resources>
    <!-- Base app theme — Theme.Material.NoTitleBar.Fullscreen was removed
         in compileSdk 35. Using Theme.AppCompat.NoActionBar instead, which
         is available on all API levels and gives the same fullscreen feel.
         Window flags for true fullscreen are set in code via WindowCompat. -->
    <style name="Theme.PdfViewer" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
