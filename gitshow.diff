commit d1ac97e64705228739403500f55b2774d390ce1a
Author: raseledutools <bhmrasel60@gmail.com>
Date:   Wed Jul 8 21:03:55 2026 +0600

    fix: Resolve PDF viewer OOM on large files and make auto-updater reliable

diff --git a/app/src/full/java/com/rasel/RasFocus/selfcontrol/study_tools/PdfViewerActivity.kt b/app/src/full/java/com/rasel/RasFocus/selfcontrol/study_tools/PdfViewerActivity.kt
index 0db31b7..1ff4604 100644
--- a/app/src/full/java/com/rasel/RasFocus/selfcontrol/study_tools/PdfViewerActivity.kt
+++ b/app/src/full/java/com/rasel/RasFocus/selfcontrol/study_tools/PdfViewerActivity.kt
@@ -86,21 +86,12 @@ private val HL_PINK    = Color(0xAAFF6B9D)
 private const val MAX_RENDER_DIM = 4096
 
 data class PageData(
-    val bitmap:    Bitmap,
     val textPage:  PdfTextPage?,
     val pageIndex: Int,
     val widthPx:   Int,
     val heightPx:  Int,
-    // FIX: "onek onek zoom" — at high zoom, a bitmap rendered once at 1x
-    // screen-width resolution just gets visually stretched and turns
-    // blurry/pixelated. Pdfium doesn't expose raw vector path data to
-    // re-draw directly (no PDF renderer on Android does), but it CAN
-    // re-rasterize at any resolution on demand — the same technique every
-    // real PDF viewer (Adobe, Chrome, etc) uses for "sharp at any zoom".
-    // This tracks which resolution tier this page's CURRENT bitmap was
-    // rendered at, so we know when the user has zoomed past that and it's
-    // time to re-render sharper.
     val renderedAtScale: Float = 1f,
+    val bitmap:    Bitmap? = null
 )
 
 data class TextSelection(
@@ -288,17 +279,10 @@ fun NativePdfViewer(uri: Uri?, fileName: String, onClose: () -> Unit) {
                     val scale = screenW.toFloat() / origW
                     val bmpW  = screenW
                     val bmpH  = (origH * scale).roundToInt().coerceAtLeast(1)
-
-                    val bmp = Bitmap.createBitmap(bmpW, bmpH, Bitmap.Config.ARGB_8888)
-                    bmp.eraseColor(AColor.WHITE)
-                    page.renderPageBitmap(bmp, 0, 0, bmpW, bmpH, true)
-
-                    // Open text page for selection
                     val textPage: PdfTextPage? = try { page.openTextPage() } catch (_: Exception) { null }
-
                     withContext(Dispatchers.Main) {
                         if (i < pages.size)
-                            pages[i] = PageData(bmp, textPage, i, bmpW, bmpH)
+                            pages[i] = PageData(textPage, i, bmpW, bmpH)
                     }
                     page.close()
                 }
@@ -312,12 +296,6 @@ fun NativePdfViewer(uri: Uri?, fileName: String, onClose: () -> Unit) {
     }
 
     // ── Re-render a page at higher resolution for sharp zoom ────────────────
-    // Reopens the page from the still-open pdfDoc (individual PdfPage handles
-    // get closed after each render to free native memory, but the document
-    // itself stays open for the viewer's lifetime — reopening one page from
-    // it is cheap). Caps the target resolution at MAX_RENDER_DIM regardless
-    // of how far the user has zoomed, so an extreme zoom on a huge page
-    // can't blow past a safe bitmap memory budget.
     suspend fun reRenderPageSharper(pageIndex: Int, targetScale: Float) {
         val doc = pdfDoc ?: return
         withContext(Dispatchers.IO) {
@@ -357,7 +335,7 @@ fun NativePdfViewer(uri: Uri?, fileName: String, onClose: () -> Unit) {
                             renderedAtScale = targetScale
                         )
                         // Free the old, now-unused lower-res bitmap.
-                        if (!oldBitmap.isRecycled) oldBitmap.recycle()
+                        if (oldBitmap != null && !oldBitmap.isRecycled) oldBitmap.recycle()
                     }
                 }
             } catch (_: Exception) {
@@ -518,6 +496,9 @@ fun NativePdfViewer(uri: Uri?, fileName: String, onClose: () -> Unit) {
                                         selection    = sel
                                         showToolbar  = sel != null
                                     },
+                                    onLoadBitmap   = { scale ->
+                                        scope.launch { reRenderPageSharper(idx, scale) }
+                                    }
                                 )
                             }
                         }
@@ -581,6 +562,7 @@ private fun PdfPageItem(
     pageData:       PageData,
     highlights:     List<Highlight>,
     onTextSelected: (TextSelection?) -> Unit,
+    onLoadBitmap:   (Float) -> Unit,
 ) {
     val density = LocalDensity.current
 
@@ -691,16 +673,26 @@ private fun PdfPageItem(
     ) {
         // ── Render page bitmap ─────────────────────────────────────────────
         androidx.compose.foundation.layout.Box(
-            Modifier.fillMaxWidth().wrapContentHeight()
+            Modifier.fillMaxWidth().height(with(density) { imgHeightPx.toDp() }).background(Color.White)
         ) {
-        Image(
-            bitmap             = pageData.bitmap.asImageBitmap(),
-            contentDescription = "Page ${pageData.pageIndex + 1}",
-            contentScale       = ContentScale.FillWidth,
-            modifier           = Modifier
-                .fillMaxWidth()
-                .wrapContentHeight()
-        )
+            LaunchedEffect(pageData.pageIndex) {
+                if (pageData.bitmap == null) {
+                    onLoadBitmap(1f)
+                }
+            }
+
+            if (pageData.bitmap != null) {
+                Image(
+                    bitmap             = pageData.bitmap.asImageBitmap(),
+                    contentDescription = "Page ${pageData.pageIndex + 1}",
+                    contentScale       = ContentScale.FillWidth,
+                    modifier           = Modifier.fillMaxWidth().wrapContentHeight()
+                )
+            } else {
+                Box(Modifier.fillMaxSize(), contentAlignment = Alignment.Center) {
+                    CircularProgressIndicator(modifier = Modifier.size(24.dp), strokeWidth = 2.dp)
+                }
+            }
 
         // ── Render highlights + selection overlay ─────────────────────────
         val allHighlights = highlights
diff --git a/app/src/main/java/com/rasel/RasFocus/AutoUpdater.kt b/app/src/main/java/com/rasel/RasFocus/AutoUpdater.kt
index d771e32..e231a10 100644
--- a/app/src/main/java/com/rasel/RasFocus/AutoUpdater.kt
+++ b/app/src/main/java/com/rasel/RasFocus/AutoUpdater.kt
@@ -114,8 +114,20 @@ object AutoUpdater {
                     
                     if (downloadUrl != null) {
                         withContext(Dispatchers.Main) {
-                            Toast.makeText(context, "Downloading $targetApkName...", Toast.LENGTH_SHORT).show()
-                            downloadUpdate(context, downloadUrl!!, targetApkName, newTag)
+                            Toast.makeText(context, "Opening update in browser...", Toast.LENGTH_SHORT).show()
+                            val intent = Intent(Intent.ACTION_VIEW, Uri.parse(downloadUrl))
+                            intent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)
+                            try {
+                                context.startActivity(intent)
+                                
+                                // Save the tag so we don't prompt again for this version
+                                context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE)
+                                    .edit()
+                                    .putString(LAST_TAG_KEY, newTag)
+                                    .apply()
+                            } catch (e: Exception) {
+                                Toast.makeText(context, "Failed to open browser.", Toast.LENGTH_SHORT).show()
+                            }
                         }
                     } else {
                         withContext(Dispatchers.Main) {
@@ -131,70 +143,4 @@ object AutoUpdater {
             }
         }
     }
-
-    private fun downloadUpdate(context: Context, downloadUrl: String, targetApkName: String, newTag: String) {
-        val subPath = "apk release/$targetApkName"
-        
-        // Remove old update file if exists
-        val oldFile = File(Environment.getExternalStoragePublicDirectory(Environment.DIRECTORY_DOWNLOADS), subPath)
-        if (oldFile.exists()) {
-            oldFile.delete()
-        }
-
-        val request = DownloadManager.Request(Uri.parse(downloadUrl))
-            .setTitle("RasFocus Update")
-            .setDescription("Downloading $targetApkName...")
-            .setNotificationVisibility(DownloadManager.Request.VISIBILITY_VISIBLE_NOTIFY_COMPLETED)
-            .setDestinationInExternalPublicDir(Environment.DIRECTORY_DOWNLOADS, subPath)
-
-        val downloadManager = context.getSystemService(Context.DOWNLOAD_SERVICE) as DownloadManager
-        val downloadId = downloadManager.enqueue(request)
-
-        val onComplete = object : BroadcastReceiver() {
-            override fun onReceive(ctxt: Context, intent: Intent) {
-                val id = intent.getLongExtra(DownloadManager.EXTRA_DOWNLOAD_ID, -1)
-                if (id == downloadId) {
-                    ctxt.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE)
-                        .edit()
-                        .putString(LAST_TAG_KEY, newTag)
-                        .apply()
-                        
-                    installUpdate(ctxt, subPath)
-                    ctxt.unregisterReceiver(this)
-                }
-            }
-        }
-        
-        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU) {
-            context.registerReceiver(onComplete, IntentFilter(DownloadManager.ACTION_DOWNLOAD_COMPLETE), Context.RECEIVER_EXPORTED)
-        } else {
-            context.registerReceiver(onComplete, IntentFilter(DownloadManager.ACTION_DOWNLOAD_COMPLETE))
-        }
-    }
-
-    private fun installUpdate(context: Context, subPath: String) {
-        val file = File(Environment.getExternalStoragePublicDirectory(Environment.DIRECTORY_DOWNLOADS), subPath)
-        if (!file.exists()) {
-            Toast.makeText(context, "Update file not found!", Toast.LENGTH_SHORT).show()
-            return
-        }
-
-        val uri = FileProvider.getUriForFile(
-            context,
-            "${context.packageName}.fileprovider",
-            file
-        )
-
-        val intent = Intent(Intent.ACTION_VIEW).apply {
-            setDataAndType(uri, "application/vnd.android.package-archive")
-            flags = Intent.FLAG_ACTIVITY_NEW_TASK or Intent.FLAG_GRANT_READ_URI_PERMISSION
-        }
-
-        try {
-            context.startActivity(intent)
-        } catch (e: Exception) {
-            Log.e(TAG, "Failed to start install intent", e)
-            Toast.makeText(context, "Failed to start installation.", Toast.LENGTH_SHORT).show()
-        }
-    }
 }
